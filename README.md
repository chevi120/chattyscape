
  # Floating Button Implementation (Copy)

  This is a code bundle for Floating Button Implementation (Copy). The original project is available at https://www.figma.com/design/oEMai1wotyu1nZM7m1cm9J/Floating-Button-Implementation--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  